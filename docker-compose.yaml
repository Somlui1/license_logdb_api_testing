
services:
  my-fastapi:
    build: .
    ports:
      - "8181:80"
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "80"]
    restart: unless-stopped
    networks:
      - postgresql_default

networks:
  postgresql_default:
    external: true
